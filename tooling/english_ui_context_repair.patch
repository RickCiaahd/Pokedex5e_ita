diff --git a/lib/screens/tools/encounter_library_screen.dart b/lib/screens/tools/encounter_library_screen.dart
index f1cc0a1..24e36a3 100644
--- a/lib/screens/tools/encounter_library_screen.dart
+++ b/lib/screens/tools/encounter_library_screen.dart
@@ -690,7 +690,7 @@ class _SavedEncounterCard extends StatelessWidget {
                       ),
                     ),
                   ),
-                  Chip(label: Text(_sourceLabel(saved.source))),
+                  Chip(label: Text(_sourceLabel(context, saved.source))),
                 ],
               ),
               const SizedBox(height: 8),
@@ -756,7 +756,8 @@ class _SavedEncounterCard extends StatelessWidget {
     return '$day/$month/${value.year}';
   }
 
-  String _sourceLabel(EncounterSource source) => switch (source) {
+  String _sourceLabel(BuildContext context, EncounterSource source) =>
+      switch (source) {
     EncounterSource.automatic => context.uiText('Automatico', 'Automatic'),
     EncounterSource.manual => context.uiText('Manuale', 'Manual'),
     EncounterSource.collection => context.uiText('Raccolta', 'Collection'),
diff --git a/lib/screens/trainer/trainer_sheet_screen.dart b/lib/screens/trainer/trainer_sheet_screen.dart
index 2428fe5..c8366f8 100644
--- a/lib/screens/trainer/trainer_sheet_screen.dart
+++ b/lib/screens/trainer/trainer_sheet_screen.dart
@@ -1846,7 +1846,7 @@ class _TrainerProgressionColumn extends StatelessWidget {
                 'Choose specialization',
               )
             : TrainerUiLocalization.specializationName(_specializationAt(0)),
-        detail: _specializationDetail(0),
+        detail: _specializationDetail(context, 0),
         onTap: () => onSpecializationTap(0),
       ),
     ];
@@ -1912,7 +1912,7 @@ class _TrainerProgressionColumn extends StatelessWidget {
                 'Choose specialization',
               )
               : TrainerUiLocalization.specializationName(_specializationAt(1)),
-          detail: _specializationDetail(1),
+          detail: _specializationDetail(context, 1),
           onTap: () => onSpecializationTap(1),
         ),
       );
@@ -1929,7 +1929,7 @@ class _TrainerProgressionColumn extends StatelessWidget {
                 'Choose specialization',
               )
               : TrainerUiLocalization.specializationName(_specializationAt(2)),
-          detail: _specializationDetail(2),
+          detail: _specializationDetail(context, 2),
           onTap: () => onSpecializationTap(2),
         ),
       );
@@ -1961,7 +1961,7 @@ class _TrainerProgressionColumn extends StatelessWidget {
     return index < specializations.length ? specializations[index] : '';
   }
 
-  String _specializationDetail(int index) {
+  String _specializationDetail(BuildContext context, int index) {
     final specialization = _specializationAt(index);
     if (specialization.isEmpty) {
       return context.uiText(
