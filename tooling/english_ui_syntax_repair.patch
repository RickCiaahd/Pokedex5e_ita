diff --git a/lib/screens/tools/encounter_result_screen.dart b/lib/screens/tools/encounter_result_screen.dart
index 57617e1..4a66e1a 100644
--- a/lib/screens/tools/encounter_result_screen.dart
+++ b/lib/screens/tools/encounter_result_screen.dart
@@ -356,11 +356,13 @@ class _EncounterResultScreenState extends State<EncounterResultScreen> {
                   const SizedBox(height: 6),
                   Text(
                     context.uiText(
-              'Obiettivo ${_encounter.targetDifficulty.label} · ',
-              'Target ${_encounter.targetDifficulty.englishLabel} · ',
-            )
-                    'budget gruppo ${_format(estimate.partyBudget)} · '
-                    'costo incontro ${_format(estimate.encounterCost)}',
+                      'Obiettivo ${_encounter.targetDifficulty.label} · '
+                          'budget gruppo ${_format(estimate.partyBudget)} · '
+                          'costo incontro ${_format(estimate.encounterCost)}',
+                      'Target ${_encounter.targetDifficulty.englishLabel} · '
+                          'party budget ${_format(estimate.partyBudget)} · '
+                          'encounter cost ${_format(estimate.encounterCost)}',
+                    ),
                     style: TextStyle(
                       color: Theme.of(context).colorScheme.onPrimaryContainer,
                     ),
@@ -368,14 +370,13 @@ class _EncounterResultScreenState extends State<EncounterResultScreen> {
                   const SizedBox(height: 4),
                   Text(
                     context.uiText(
-              '${_encounter.party.activePokemon} Pokémon alleati · ',
-              '${_encounter.party.activePokemon} allied Pokémon · ',
-            )
-                    context.uiText(
-              'livello medio ${_encounter.party.averageLevel} · ',
-              'average level ${_encounter.party.averageLevel} · ',
-            )
-                    '${_encounter.members.length} avversari',
+                      '${_encounter.party.activePokemon} Pokémon alleati · '
+                          'livello medio ${_encounter.party.averageLevel} · '
+                          '${_encounter.members.length} avversari',
+                      '${_encounter.party.activePokemon} allied Pokémon · '
+                          'average level ${_encounter.party.averageLevel} · '
+                          '${_encounter.members.length} opponents',
+                    ),
                     style: TextStyle(
                       color: Theme.of(context).colorScheme.onPrimaryContainer,
                     ),
@@ -676,10 +677,11 @@ class _EncounterMemberCard extends StatelessWidget {
         ),
         subtitle: Text(
           context.uiText(
-              '${generated.formLabel} · Livello ${generated.level} · ',
-              '${generated.formLabel} · Level ${generated.level} · ',
-            )
-          'SR ${pokemon.sr} · PF ${generated.maxHp} · CA $armorClass',
+            '${generated.formLabel} · Livello ${generated.level} · '
+                'SR ${pokemon.sr} · PF ${generated.maxHp} · CA $armorClass',
+            '${generated.formLabel} · Level ${generated.level} · '
+                'SR ${pokemon.sr} · HP ${generated.maxHp} · AC $armorClass',
+          ),
         ),
         childrenPadding: const EdgeInsets.fromLTRB(16, 0, 16, 14),
         children: [
diff --git a/lib/screens/tools/npc_trainer_result_screen.dart b/lib/screens/tools/npc_trainer_result_screen.dart
index d853096..155f261 100644
--- a/lib/screens/tools/npc_trainer_result_screen.dart
+++ b/lib/screens/tools/npc_trainer_result_screen.dart
@@ -775,10 +775,11 @@ class _NpcPokemonCard extends StatelessWidget {
         ),
         subtitle: Text(
           context.uiText(
-              '${generated.formLabel} · Livello ${generated.level} · ',
-              '${generated.formLabel} · Level ${generated.level} · ',
-            )
-          'SR ${pokemon.sr} · PF ${generated.maxHp} · CA $armorClass',
+            '${generated.formLabel} · Livello ${generated.level} · '
+                'SR ${pokemon.sr} · PF ${generated.maxHp} · CA $armorClass',
+            '${generated.formLabel} · Level ${generated.level} · '
+                'SR ${pokemon.sr} · HP ${generated.maxHp} · AC $armorClass',
+          ),
         ),
         childrenPadding: const EdgeInsets.fromLTRB(16, 0, 16, 14),
         children: [
