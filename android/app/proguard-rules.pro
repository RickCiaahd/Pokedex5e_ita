# Trainer Atlas 5e currently needs no app-specific keep rules.
#
# Flutter plugins are referenced by GeneratedPluginRegistrant and are therefore
# visible to R8. Keep this file intentionally narrow: broad -keep rules would
# make the shrinking test pass without proving that minification is effective.
# Add a targeted rule here only when a reproducible release-only failure shows
# that a library relies on Android-side reflection.
