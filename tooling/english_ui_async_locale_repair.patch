diff --git a/lib/screens/tools/encounter_library_screen.dart b/lib/screens/tools/encounter_library_screen.dart
index 24e36a3..ec64ce5 100644
--- a/lib/screens/tools/encounter_library_screen.dart
+++ b/lib/screens/tools/encounter_library_screen.dart
@@ -126,7 +126,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       );
       if (!mounted || !launched) return;
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           'Il fight di ${saved.name} è stato salvato e può essere ripreso dagli Strumenti del Master.',
           'The fight for ${saved.name} was saved and can be resumed from Master Tools.',
         ),
@@ -153,7 +153,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       await _repository.saveEncounter(profileId: profile.id, encounter: copy);
       await _load();
       _setMessage(
-        context.uiText('${copy.name} duplicato.', '${copy.name} duplicated.'),
+        uiTextForLanguage('${copy.name} duplicato.', '${copy.name} duplicated.'),
       );
     } catch (error) {
       _setMessage(_friendlyError(error), isError: true);
@@ -169,10 +169,10 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       context: context,
       builder: (_) => AlertDialog(
         title: Text(
-          context.uiText('Eliminare l’incontro?', 'Delete encounter?'),
+          uiTextForLanguage('Eliminare l’incontro?', 'Delete encounter?'),
         ),
         content: Text(
-          context.uiText(
+          uiTextForLanguage(
             '“${saved.name}” verrà rimosso dalla libreria.',
             '“${saved.name}” will be removed from the library.',
           ),
@@ -180,11 +180,11 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
         actions: [
           TextButton(
             onPressed: () => Navigator.of(context).pop(false),
-            child: Text(context.uiText('Annulla', 'Cancel')),
+            child: Text(uiTextForLanguage('Annulla', 'Cancel')),
           ),
           FilledButton(
             onPressed: () => Navigator.of(context).pop(true),
-            child: Text(context.uiText('Elimina', 'Delete')),
+            child: Text(uiTextForLanguage('Elimina', 'Delete')),
           ),
         ],
       ),
@@ -199,7 +199,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       );
       await _load();
       _setMessage(
-        context.uiText('${saved.name} eliminato.', '${saved.name} deleted.'),
+        uiTextForLanguage('${saved.name} eliminato.', '${saved.name} deleted.'),
       );
     } catch (error) {
       _setMessage(_friendlyError(error), isError: true);
@@ -210,7 +210,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
 
   Future<CampaignTransferBundle?> _pickTransferFile() async {
     final result = await FilePicker.platform.pickFiles(
-      dialogTitle: context.uiText(
+      dialogTitle: uiTextForLanguage(
         'Importa incontro Trainer Atlas 5e',
         'Import Trainer Atlas 5e encounter',
       ),
@@ -236,7 +236,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       );
       final json = await _transferService.encodePortable(bundle);
       final path = await FilePicker.platform.saveFile(
-        dialogTitle: context.uiText(
+        dialogTitle: uiTextForLanguage(
           'Esporta ${saved.name}',
           'Export ${saved.name}',
         ),
@@ -247,8 +247,8 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       );
       _setMessage(
         path == null
-            ? context.uiText('Esportazione annullata.', 'Export cancelled.')
-            : context.uiText(
+            ? uiTextForLanguage('Esportazione annullata.', 'Export cancelled.')
+            : uiTextForLanguage(
                 '${saved.name} esportato correttamente.',
                 '${saved.name} exported successfully.',
               ),
@@ -276,12 +276,12 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
         content: json,
         fileName: _transferService.fileNameForEncounter(bundle),
         mimeType: 'application/json',
-        title: context.uiText(
+        title: uiTextForLanguage(
           'Condividi ${saved.name}',
           'Share ${saved.name}',
         ),
         subject: '${saved.name} · Trainer Atlas 5e',
-        text: context.uiText(
+        text: uiTextForLanguage(
           'Incontro esportato da Trainer Atlas 5e.',
           'Encounter exported from Trainer Atlas 5e.',
         ),
@@ -289,7 +289,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       _setMessage(
         _shareService.feedback(
           outcome,
-          successMessage: context.uiText(
+          successMessage: uiTextForLanguage(
             '${saved.name} condiviso correttamente.',
             '${saved.name} shared successfully.',
           ),
@@ -309,12 +309,12 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
     try {
       final bundle = await _pickTransferFile();
       if (bundle == null) {
-        _setMessage(context.uiText('Importazione annullata.', 'Import cancelled.'));
+        _setMessage(uiTextForLanguage('Importazione annullata.', 'Import cancelled.'));
         return;
       }
       if (bundle.kind != CampaignTransferKind.encounter) {
         throw FormatException(
-          context.uiText(
+          uiTextForLanguage(
             'Seleziona un file esportato come incontro.',
             'Select a file exported as an encounter.',
           ),
@@ -324,7 +324,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       if (!mounted) return;
       final origin = bundle.sourceProfileName.isEmpty
           ? ''
-          : context.uiText(
+          : uiTextForLanguage(
               ' dal profilo ${bundle.sourceProfileName}',
               ' from profile ${bundle.sourceProfileName}',
             );
@@ -332,10 +332,10 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
         context: context,
         builder: (_) => AlertDialog(
           title: Text(
-            context.uiText('Importare incontro?', 'Import encounter?'),
+            uiTextForLanguage('Importare incontro?', 'Import encounter?'),
           ),
           content: Text(
-            context.uiText(
+            uiTextForLanguage(
               'Vuoi importare “${source.name}”$origin con ${source.enemyCount} avversari? Verrà creata una nuova copia nella libreria del profilo attivo.',
               'Import “${source.name}”$origin with ${source.enemyCount} opponents? A new copy will be created in the active profile library.',
             ),
@@ -343,17 +343,17 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
           actions: [
             TextButton(
               onPressed: () => Navigator.of(context).pop(false),
-              child: Text(context.uiText('ANNULLA', 'CANCEL')),
+              child: Text(uiTextForLanguage('ANNULLA', 'CANCEL')),
             ),
             FilledButton(
               onPressed: () => Navigator.of(context).pop(true),
-              child: Text(context.uiText('IMPORTA', 'IMPORT')),
+              child: Text(uiTextForLanguage('IMPORTA', 'IMPORT')),
             ),
           ],
         ),
       );
       if (confirmed != true) {
-        _setMessage(context.uiText('Importazione annullata.', 'Import cancelled.'));
+        _setMessage(uiTextForLanguage('Importazione annullata.', 'Import cancelled.'));
         return;
       }
       final imported = await _transferService.importEncounter(
@@ -363,7 +363,7 @@ class _EncounterLibraryScreenState extends State<EncounterLibraryScreen> {
       );
       await _load();
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           '${imported.name} importato nella libreria incontri.',
           '${imported.name} imported into the encounter library.',
         ),
diff --git a/lib/screens/tools/npc_trainer_library_screen.dart b/lib/screens/tools/npc_trainer_library_screen.dart
index 50a9ad5..fb12d8f 100644
--- a/lib/screens/tools/npc_trainer_library_screen.dart
+++ b/lib/screens/tools/npc_trainer_library_screen.dart
@@ -150,7 +150,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
 
   Future<CampaignTransferBundle?> _pickTransferFile() async {
     final result = await FilePicker.platform.pickFiles(
-      dialogTitle: context.uiText(
+      dialogTitle: uiTextForLanguage(
         'Importa Allenatore PNG Trainer Atlas 5e',
         'Import NPC Trainer · Trainer Atlas 5e',
       ),
@@ -176,7 +176,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       );
       final json = await _transferService.encodePortable(bundle);
       final path = await FilePicker.platform.saveFile(
-        dialogTitle: context.uiText(
+        dialogTitle: uiTextForLanguage(
           'Esporta ${saved.displayName}',
           'Export ${saved.displayName}',
         ),
@@ -187,8 +187,8 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       );
       _setMessage(
         path == null
-            ? context.uiText('Esportazione annullata.', 'Export cancelled.')
-            : context.uiText(
+            ? uiTextForLanguage('Esportazione annullata.', 'Export cancelled.')
+            : uiTextForLanguage(
                 '${saved.displayName} esportato correttamente.',
                 '${saved.displayName} exported successfully.',
               ),
@@ -216,12 +216,12 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
         content: json,
         fileName: _transferService.fileNameForNpcTrainer(bundle),
         mimeType: 'application/json',
-        title: context.uiText(
+        title: uiTextForLanguage(
           'Condividi ${saved.displayName}',
           'Share ${saved.displayName}',
         ),
         subject: '${saved.displayName} · Trainer Atlas 5e',
-        text: context.uiText(
+        text: uiTextForLanguage(
           'Allenatore PNG esportato da Trainer Atlas 5e.',
           'NPC Trainer exported from Trainer Atlas 5e.',
         ),
@@ -229,7 +229,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       _setMessage(
         _shareService.feedback(
           outcome,
-          successMessage: context.uiText(
+          successMessage: uiTextForLanguage(
             '${saved.displayName} condiviso correttamente.',
             '${saved.displayName} shared successfully.',
           ),
@@ -249,12 +249,12 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
     try {
       final bundle = await _pickTransferFile();
       if (bundle == null) {
-        _setMessage(context.uiText('Importazione annullata.', 'Import cancelled.'));
+        _setMessage(uiTextForLanguage('Importazione annullata.', 'Import cancelled.'));
         return;
       }
       if (bundle.kind != CampaignTransferKind.npcTrainer) {
         throw FormatException(
-          context.uiText(
+          uiTextForLanguage(
             'Seleziona un file esportato come Allenatore PNG.',
             'Select a file exported as an NPC Trainer.',
           ),
@@ -264,7 +264,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       if (!mounted) return;
       final origin = bundle.sourceProfileName.isEmpty
           ? ''
-          : context.uiText(
+          : uiTextForLanguage(
               ' dal profilo ${bundle.sourceProfileName}',
               ' from profile ${bundle.sourceProfileName}',
             );
@@ -272,10 +272,10 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
         context: context,
         builder: (_) => AlertDialog(
           title: Text(
-            context.uiText('Importare Allenatore PNG?', 'Import NPC Trainer?'),
+            uiTextForLanguage('Importare Allenatore PNG?', 'Import NPC Trainer?'),
           ),
           content: Text(
-            context.uiText(
+            uiTextForLanguage(
               'Vuoi importare “${source.displayName}”$origin con ${source.team.length} Pokémon? Verrà creata una nuova copia nella libreria del profilo attivo.',
               'Import “${source.displayName}”$origin with ${source.team.length} Pokémon? A new copy will be created in the active profile library.',
             ),
@@ -287,17 +287,17 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
           actions: [
             TextButton(
               onPressed: () => Navigator.of(context).pop(false),
-              child: Text(context.uiText('ANNULLA', 'CANCEL')),
+              child: Text(uiTextForLanguage('ANNULLA', 'CANCEL')),
             ),
             FilledButton(
               onPressed: () => Navigator.of(context).pop(true),
-              child: Text(context.uiText('IMPORTA', 'IMPORT')),
+              child: Text(uiTextForLanguage('IMPORTA', 'IMPORT')),
             ),
           ],
         ),
       );
       if (confirmed != true) {
-        _setMessage(context.uiText('Importazione annullata.', 'Import cancelled.'));
+        _setMessage(uiTextForLanguage('Importazione annullata.', 'Import cancelled.'));
         return;
       }
       final imported = await _transferService.importNpcTrainer(
@@ -307,7 +307,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       );
       await _load();
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           '${imported.displayName} importato nella libreria Allenatori PNG.',
           '${imported.displayName} imported into the NPC Trainer library.',
         ),
@@ -334,7 +334,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       await _repository.saveTrainer(profileId: profile.id, trainer: copy);
       await _load();
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           '${copy.displayName} duplicato.',
           '${copy.displayName} duplicated.',
         ),
@@ -353,10 +353,10 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       context: context,
       builder: (_) => AlertDialog(
         title: Text(
-          context.uiText('Eliminare l’allenatore?', 'Delete trainer?'),
+          uiTextForLanguage('Eliminare l’allenatore?', 'Delete trainer?'),
         ),
         content: Text(
-          context.uiText(
+          uiTextForLanguage(
             '“${saved.displayName}” verrà rimosso dalla libreria. Le sessioni di fight già avviate non verranno modificate.',
             '“${saved.displayName}” will be removed from the library. Existing battle sessions will not be changed.',
           ),
@@ -364,11 +364,11 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
         actions: [
           TextButton(
             onPressed: () => Navigator.of(context).pop(false),
-            child: Text(context.uiText('Annulla', 'Cancel')),
+            child: Text(uiTextForLanguage('Annulla', 'Cancel')),
           ),
           FilledButton(
             onPressed: () => Navigator.of(context).pop(true),
-            child: Text(context.uiText('Elimina', 'Delete')),
+            child: Text(uiTextForLanguage('Elimina', 'Delete')),
           ),
         ],
       ),
@@ -384,7 +384,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       _selectedIds.remove(saved.id);
       await _load();
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           '${saved.displayName} eliminato.',
           '${saved.displayName} deleted.',
         ),
@@ -403,7 +403,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
     if (!mounted) return;
     if (session == null) {
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           'Non c’è nessun fight del Master da riprendere.',
           'There is no Game Master battle to resume.',
         ),
@@ -440,10 +440,10 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
     if (!mounted || trainers.isEmpty) return;
     final config = await showDialog<_FightConfig>(
       context: context,
-      builder: (dialogContext) => _FightConfigDialog(
+      builder: (_) => _FightConfigDialog(
         trainerCount: trainers.length,
-        activePokemonLabel: dialogContext.uiText(
+        activePokemonLabel: uiTextForLanguage(
           'Pokémon attivi per Allenatore',
           'Active Pokémon per Trainer',
         ),
@@ -471,12 +471,12 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
       );
       await _load();
       _setMessage(
-        context.uiText(
+        uiTextForLanguage(
           '${trainers.length == 1 ? trainers.first.displayName : '${trainers.length} Allenatori'} entrati nel fight con $activeLabel.',
           '${trainers.length == 1 ? trainers.first.displayName : '${trainers.length} Trainers'} entered the battle with $activeLabel.',
         ),
       );
     } catch (error) {
       _setMessage(_friendlyError(error), isError: true);
@@ -487,7 +487,7 @@ class _NpcTrainerLibraryScreenState extends State<NpcTrainerLibraryScreen> {
 
   String _activePokemonSummary(int activePokemonCount) {
     if (activePokemonCount == 0) {
-      return context.uiText('l’intera squadra', 'the full team');
+      return uiTextForLanguage('l’intera squadra', 'the full team');
     }
-    return context.uiText(
+    return uiTextForLanguage(
       '$activePokemonCount Pokémon attivi',
       '$activePokemonCount active Pokémon',
     );
diff --git a/lib/screens/tools/pokemon_generator_screen.dart b/lib/screens/tools/pokemon_generator_screen.dart
index 0554ce3..37cc130 100644
--- a/lib/screens/tools/pokemon_generator_screen.dart
+++ b/lib/screens/tools/pokemon_generator_screen.dart
@@ -443,7 +443,7 @@ class _PokemonGeneratorScreenState extends State<PokemonGeneratorScreen> {
           ),
         );
-        destination = context.uiText(
+        destination = uiTextForLanguage(
           'aggiunto allo slot squadra ${freeSlot.slotIndex + 1}',
           'added to team slot ${freeSlot.slotIndex + 1}',
         );
@@ -461,12 +461,12 @@ class _PokemonGeneratorScreenState extends State<PokemonGeneratorScreen> {
           abilities: generated.ability == null
               ? const []
               : [generated.ability!],
-          notes: context.uiText(
+          notes: uiTextForLanguage(
             'Generato dagli Strumenti al livello ${generated.level}.',
             'Generated by Tools at level ${generated.level}.',
           ),
         );
-        destination = context.uiText('inviato al PC', 'sent to the PC');
+        destination = uiTextForLanguage('inviato al PC', 'sent to the PC');
       }
 
       final team = await _teamRepository.getTeam(profile.id);
@@ -564,7 +564,7 @@ class _PokemonGeneratorScreenState extends State<PokemonGeneratorScreen> {
               abilities: generated.ability == null
                   ? const []
                   : [generated.ability!],
-              notes: context.uiText(
+              notes: uiTextForLanguage(
                 'Generato dagli Strumenti al livello ${generated.level}.',
                 'Generated by Tools at level ${generated.level}.',
               ),
@@ -592,7 +592,7 @@ class _PokemonGeneratorScreenState extends State<PokemonGeneratorScreen> {
         _generatedBatch = const [];
         _selectedPokemonIds.clear();
-        _statusMessage = context.uiText(
+        _statusMessage = uiTextForLanguage(
           '${generatedBatch.length} Pokémon aggiunti: $teamCount in squadra, ${pcAdditions.length} nel PC.',
           '${generatedBatch.length} Pokémon added: $teamCount to the team, ${pcAdditions.length} to the PC.',
         );
