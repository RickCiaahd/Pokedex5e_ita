name: Held item audit

on:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tool/temporary_extract_held_items.py
      - uses: actions/upload-artifact@v4
        with:
          name: held-items-audit
          path: held-items-audit.json
